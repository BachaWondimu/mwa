
export class Data2 {
    // data structure 2
    #data: { [id: string]: string; } = {}; // Record<string, string>

    // consider the item id is used as key, and the item title is the value
    // add item (if id does not exist)
    // update the item title (if id exist)
    // delete item (if id exist)
    // read title by id (if id exist)

}